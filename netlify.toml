# Minimal redirects — let Netlify serve static files by default
# Only add explicit redirects where needed

# Audit tool paths
[[redirects]]
  from = "/audit"
  to = "/audit/index.html"
  status = 200

[[redirects]]
  from = "/audit/"
  to = "/audit/index.html"
  status = 200
